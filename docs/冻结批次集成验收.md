# 当前冻结批次的工程集成与验收

本轮沿用 A 的发布规则、B 的 1000 条冻结 VAE 候选及 C 的 QC/全局比对/复核交接。
不重新训练、不重新生成、不把生成失败样本用补采隐藏。参考 release 保留暂定状态。
本次“完成”指冻结交接文件到评价、筛选和实验输出的工程链路，不指生物学功能验证。

## 总入口

```bash
git fetch origin
python experiments/run_full_experiment.py --output /tmp/gv02_03_v2_run
```

默认流程是 `frozen-v2`，配置为 `configs/d_evaluation.json`。
运行前校验配置和 Git 源提交，运行后核验候选池、名单、矩阵、实验数量与全部产物哈希，
再独立复跑比较 Manifest。输出目录必须为新的或空目录。
流程不需要 GPU、Torch 或 checkpoint，不重新调用 HMMER/BLAST/MAFFT。

检查已经提交的产物：

```bash
python experiments/run_full_experiment.py --verify-existing
```

生成两个同级文件：

- `d_evaluation_member_a_v1_acceptance.json`：输入到筛选输出的工程验收及总入口/集成模块哈希。
- `d_evaluation_member_a_v1_reproducibility.json`：44 个产物的独立复跑哈希比较。

D 原入口继续可用：`experiments/run_d_evaluation.py`。
旧 T05 流程使用 `--workflow legacy` 或显式传入原 `.yaml` 配置，原命令保持兼容。

## 依赖与隔离验证

`requirements-frozen-v2.txt` 固定本轮实际使用的最小 Python 依赖版本。
原 `environment.yml` 继续服务含序列工具和报告工具的完整历史流程，不把两者混称为同一环境。

安装最小环境的命令：

```bash
python3.11 -m venv .venv-frozen
.venv-frozen/bin/python -m pip install -r requirements-frozen-v2.txt
.venv-frozen/bin/python experiments/run_full_experiment.py --output /tmp/gv02_03_v2_run
```

本机可在已具备这些依赖的解释器下执行隔离源代码检查：

```bash
python experiments/check_frozen_checkout.py
```

该脚本创建独立 Git 副本和新的虚拟环境，加入当前待提交补丁，清除 `PYTHONPATH`、
禁用用户 site-packages 与 pytest 外部插件，运行总入口和全部仓库测试，再与发布产物比较。
虚拟环境使用 `--system-site-packages` 继承所选解释器的依赖；这是源目录和入口隔离验证，
不是从网络全新安装依赖的证明。没有将测试通过扩大解释为任意电脑均可直接运行。
证据文件为 `results/gv02_03_v2/frozen_checkout_acceptance.json`，写明实际隔离范围和补丁哈希。

## 当前结果与解释

候选漏斗为 1000 条原始 → 147 条 GvpA 支持 → 136 条联合门槛合格 → 114 条四维证据充分。
三种策略均有 Top-10/20/50，未解析距离不填成最大值，50% 配对覆盖工程门槛另做敏感性分析。
C 的预警/人工复核标记保留在名单中，不能用“通过 QC”替代结构、组装或功能验证。

已完成的范围：输入版本冻结和哈希审计、配置校验、总入口集成、四维筛选、实验、产物校验、
同环境复现与隔离副本测试、技术进度和交接说明更新。
不重放 B 的训练与采样、不重复执行 C 的外部工具；上游证据以固定提交和哈希接收。

代码和证据需提交到当前协作分支，再按团队正常流程审查合入主分支。
真实人员、会议、工时等过程资料仍须成员本人提供；PPT 与答辩材料不在本轮范围。
